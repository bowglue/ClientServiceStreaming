export interface SliderReactiveCssInterface {
  sliderPadding: string;
  itemPadding: string;
  buttonWidth: string;
  cardWidth: string;
}

export interface SliderReactiveInfoInterface {
  cardsPerPage: number;
  cardWidth: number;
  translateLength: number;
  prevCardsPerPage: number;
}
